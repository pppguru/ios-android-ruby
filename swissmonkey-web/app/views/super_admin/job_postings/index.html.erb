<p id="notice"><%= notice %></p>

<h1>Job Postings</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_postings.each do |job_posting| %>
      <tr>
        <td><%= link_to 'Show', [:super_admin, job_posting] %></td>
        <td><%= link_to 'Edit', edit_super_admin_job_posting_path(job_posting) %></td>
        <td><%= link_to 'Destroy', [:super_admin, job_posting], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Job Posting', new_super_admin_job_posting_path %>
