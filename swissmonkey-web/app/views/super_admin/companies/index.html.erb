<p id="notice"><%= notice %></p>

<h1>Companies</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= link_to 'Show', [:super_admin, company] %></td>
        <td><%= link_to 'Edit', edit_super_admin_company_path(company) %></td>
        <td><%= link_to 'Destroy', [:super_admin, company], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company', new_super_admin_company_path %>
